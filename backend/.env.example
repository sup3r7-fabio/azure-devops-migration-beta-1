# Azure DevOps PAT (Personal Access Token) with appropriate scopes (e.g., Work Items Read)
AZDO_PAT=YOUR_PAT_HERE
# Organization name (the part after dev.azure.com/)
AZDO_ORG=your-org
# Optional default project name
AZDO_PROJECT=your-project
# Port for this proxy server
PORT=4000
# Comma separated allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000
# Log level: debug|info|warn|error
LOG_LEVEL=info
